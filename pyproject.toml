[project]
name = "spain2026"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mgdio",
    "pandas>=3.0.3",
]

[tool.uv.sources]
mgdio = { git = "ssh://git@github.com/mdinunzio/mgdio.git" }

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
